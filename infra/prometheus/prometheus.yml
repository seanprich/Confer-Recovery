global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    instance: 'confer-chapter'

scrape_configs:
  - job_name: 'confer-server'
    static_configs:
      - targets: ['server:8080']
    metrics_path: '/metrics'
    scheme: http
    scrape_timeout: 10s
